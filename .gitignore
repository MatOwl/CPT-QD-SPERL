__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Vendored / reference material outside git's concern
/paperRef
/CumSPERL_ref

# All experiment outputs go under runs/. Bare `results/` is kept so that
# legacy code paths writing into `<env>/results/` (see agents/record_csv.py)
# also stay untracked.
/runs/
results/

# Local profile dumps
*.prof

# IDE / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db
/.claude
/.ipynb_checkpoints
